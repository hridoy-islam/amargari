/* eslint-disable no-undef */
export const Home = () => {
  return (
    <div>
      <img
        className="w-full h-screen"
        src={`${import.meta.env.BASE_URL}threejs.png`}
      />
    </div>
  );
};
